# cycle-dashboard-glance

Phone-friendly glance card for the SPX Stage 12 cycle strategy. A single static HTML page that fetches `state.json` from GitHub on each page load and renders the current action, leverage, market data, and commentary.

Deployed on [Netlify](https://netlify.com) — deploys once, never rebuilds. Auto-refreshes every 5 minutes.
